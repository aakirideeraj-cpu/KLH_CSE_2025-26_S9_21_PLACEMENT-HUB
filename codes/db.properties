# Placement Hub Database Configuration
# You can modify these settings to match your MySQL Server credentials.
# If MySQL connection fails, Placement Hub will automatically use embedded SQLite (placement_hub.db)

mysql.host=localhost
mysql.port=3306
mysql.database=placement_hub
mysql.user=root
mysql.password=root
